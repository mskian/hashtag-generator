name: build-script

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3.5.0
    - uses: actions/setup-node@v3.6.0
      with:
         node-version: '18.x'
         cache: 'yarn'
    - name: use yarn
      run: |
       yarn install
       yarn css
       yarn build